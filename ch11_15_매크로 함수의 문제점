#include <stdio.h>
#define MUL(x,y) x*y
int mul(x,y);

int main() {
    int a, b;
    printf("두 개의 정수를 입력하세요: ");
    scanf("%d %d", &a, &b);
    printf("매크로함수호출결과: %d\n", MUL(a+1,b+1));
    printf("일반함수호출결과: %d\n", mul(a+1,b+1));
    return 0;
}
int mul(x,y){
    return x*y;
