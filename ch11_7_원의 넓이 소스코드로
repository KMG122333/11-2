#include <stdio.h>

int main(){
    double area, circum, radius;
    printf("반지름을 입력하시오: ");
    scanf("%lf", &radius);
    area = 3.14 * radius * radius;
    circum = 2 * 3.14 * radius;
    printf("원의 넓이 : %lf \n", area);
    printf("원의 둘레 : %lf \n", circum);
    return 0;
}

